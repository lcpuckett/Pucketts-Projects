-- create a database 
create database AUG2022Project;
-- make all the tables, check "data types"
-- figure out primary key etc to creating database
create table Table1_plants(
plantID int,
plantName varchar(255),
species varchar(255),
length varchar(255))

-- maybe use a preloaded database and just do some good queries on it lol


